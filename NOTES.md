# Teaching Notes

- First lesson should stay source-grounded and pipeline-first. The user asked to explain `torchtitan/experiments/rl`, so start with package boundaries before algorithm details.
- Use Emacs Org mode (`.org`) for lesson and reference artifacts in this workspace, not HTML. Prefer headings and bullets over Org tables because table rendering can be mangled.
- Future lessons should likely drill into DAPO-Math sample flow, weight sync/policy age, and batch-invariant parity.
